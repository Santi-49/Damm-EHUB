# Persist to disk so tokens survive container restarts
save 60 1
appendonly yes
appendfilename "appendonly.aof"

# Security: no remote access without auth (set a password via env in production)
# requirepass changeme

# Memory policy: never evict (tokens must not silently disappear)
maxmemory-policy noeviction
