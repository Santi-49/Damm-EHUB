# `edge_cost_train` — merged into `wo_changeovers.csv`

> This data product no longer exists as a separate file.
>
> [`wo_changeovers.csv`](./wo_changeovers.md) is the historical transition table.
> It contains `sku_from_id`, `sku_to_id`, SKU attribute features, change flags,
> context features, and `estimated_changeover_hours` joined from
> [`changeover_costs.csv`](./changeover_costs.md).
>
> There is currently no reliable observed changeover-duration target in the raw
> exports. If a true observed duration source is provided later, create a new
> training product rather than treating theoretical CF costs as observations.
